# Use mock API (dummy data, no network). Set to false to use real API.
VITE_USE_MOCK_API=false

# API base URL when using real API (include /api). Ensure server is running on this host/port.
VITE_API_URL=http://localhost:3000/api
